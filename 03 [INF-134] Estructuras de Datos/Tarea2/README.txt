==========
README
==========

Nota: Los programas se crearon en un entorno de Linux, los archivos
      que se utilizaron para testeo tienen terminaciones
      de linea de tipo LF (\n).

Nota: Por temas de tiempo no pudimos agregar documentacion extensa a
      absolutamente todas las operaciones de TDA de estructuras de datos,
      como el de arboles binarios o el de listas enlazadas. Pero si poseen un
      muy breve resumen de su funcionamiento.
      Sin embargo, si agregamos documentacion extensa a todas las
      funciones externas que son de utilizad y que ademas utilizamos en cada 'main.c'.

==========

Problema 1:

- El archivo 'main.c' es el que posee el codigo principal, y el que deberia compilarse.

    - El programa se ha compilado con el siguiente comando: gcc main.c -o main -Wall

    - 'main' necesita 1 parametro:

        - El nombre del archivo a leer con las operaciones a realizar.

          Ejemplo: ./main input1.dat

- El resto de archivos '.c' son dependencias de 'main.c', poseen el codigo de las funciones que se utilizaran.

(PD):
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault
segmentation fault segmentation fault segmentation fault segmentation fault

==========

Problema 2:

- El archivo 'main.c' es el que posee el codigo principal, y el que deberia compilarse.

    - El programa se ha compilado con el siguiente comando: gcc main.c -o main -Wall

    - 'main' necesita 1 parametro:

        - El nombre del archivo a leer con las operaciones a realizar.

          Ejemplo: ./main input.txt

- El resto de archivos '.c' son dependencias de 'main.c', poseen el codigo de las
  funciones que se utilizaran.
